A static site server for Github Pages, but using Pelican so I can learn that.

The static site will only serve a redirect (for now), acting as a url link shortener / short-cutter / tracker (eventually)
