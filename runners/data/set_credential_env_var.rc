#!/usr/bin/env bash

#This is meant to be sourced from the running environment from the root of the repository.

export GOOGLE_CLOUD_CREDENTIALS=$(cat runners/data/un_checked_in_google_credentials.json)
